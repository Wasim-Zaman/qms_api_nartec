<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Products List</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 20px;
            color: #333;
        }
        .header {
            text-align: center;
            margin-bottom: 30px;
            padding-bottom: 20px;
            border-bottom: 2px solid #00a86b;
        }
        .logo {
            width: 120px;
            margin-bottom: 10px;
        }
        .company-info {
            margin-bottom: 20px;
            color: #005999;
        }
        .products-table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
            font-size: 12px;
        }
        .products-table th {
            background-color: #005999;
            color: white;
            padding: 10px;
            text-align: left;
        }
        .products-table td {
            padding: 8px;
            border-bottom: 1px solid #ddd;
        }
        .products-table tr:nth-child(even) {
            background-color: #f9f9f9;
        }
        .footer {
            margin-top: 30px;
            text-align: center;
            font-size: 12px;
            color: #666;
            padding-top: 20px;
            border-top: 1px solid #ddd;
        }
        .product-image {
            width: 50px;
            height: 50px;
            object-fit: cover;
        }
        .status-active {
            color: #00a86b;
            font-weight: bold;
        }
        .status-inactive {
            color: #ff4444;
            font-weight: bold;
        }
    </style>
</head>
<body>
    <div class="header">
        <img src="https://gstsa1.org/assets/Logo-tOwwGU03.png" alt="GST Logo" class="logo">
        <h1>Products List</h1>
        <div class="company-info">
            <h3><%= user.companyNameEn %></h3>
            <p>Generated on: <%= new Date().toLocaleDateString() %></p>
        </div>
    </div>

    <table class="products-table">
        <thead>
            <tr>
                <th>Title</th>
                <th>SKU</th>
                <th>GTIN</th>
                <th>Brand</th>
                <th>Status</th>
                <th>Type</th>
                <th>Created</th>
            </tr>
        </thead>
        <tbody>
            <% products.forEach(product => { %>
                <tr>
                    <td><%= product.title %></td>
                    <td><%= product.sku %></td>
                    <td><%= product.gtin %></td>
                    <td><%= product.brandName || '-' %></td>
                    <td class="status-<%= product.status.toLowerCase() %>"><%= product.status %></td>
                    <td><%= product.productType || '-' %></td>
                    <td><%= new Date(product.createdAt).toLocaleDateString() %></td>
                </tr>
            <% }); %>
        </tbody>
    </table>

    <div class="footer">
        <p>Generated by GST - Global Standard for Technology Solution</p>
    </div>
</body>
</html> 